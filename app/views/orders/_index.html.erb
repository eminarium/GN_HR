<% @orders.each do |order| %>
    <tr>
      <td><%= order.order_no %></td>
      <td><%= order.order_date.strftime("%d-%m-%Y") %></td>
      <td><%= order.order_title %></td>
      <td><%= order.notes %></td>
      <td><%= link_to "Giňişleýin", order, class: "btn btn-info"  %></td>
      <td><%= link_to "Üýtget", edit_order_path(order), remote: true, class: "btn btn-warning" %></td>
      <td><%= link_to "Boz", order_delete_path(order), remote: true, class: "btn btn-danger" %></td>
    </tr>
<% end %>
