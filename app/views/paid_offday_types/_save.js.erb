$("ul.errors").html("")
<% if @paid_offday_type.errors.any? %>
    <% @paid_offday_type.errors.full_messages.each do |message| %>
        $("ul.errors").append($("<li />").html("<%= message.html_safe %>"))
    <% end %>
<% else %>
    $(".paid_offday_type-index").html("<%= escape_javascript(render 'index') %>")
    $("#paid_offday_type-modal").modal("hide")
<% end %>
